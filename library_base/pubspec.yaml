name: library_base
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.19.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # 国际化库
  flutter_localizations:
    sdk: flutter

  path_provider: ^2.0.13

  decimal: ^2.3.2
  # 屏幕适配方案 https://pub.dev/packages/flutter_screenutil
  flutter_screenutil: ^5.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: